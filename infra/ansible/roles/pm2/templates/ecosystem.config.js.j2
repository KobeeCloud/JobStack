// PM2 ecosystem config — managed by Ansible
// App: {{ app_name }} | Environment: {{ environment }}

module.exports = {
  apps: [
    {
      name: "{{ app_name }}",
      script: "{{ app_dir }}/current/server.js",
      cwd: "{{ app_dir }}/current",
      instances: "max",        // Use all CPU cores
      exec_mode: "cluster",
      watch: false,
      max_memory_restart: "1G",

      env: {
        NODE_ENV: "production",
        PORT: {{ app_port }},
        HOSTNAME: "0.0.0.0",
      },

      // Log configuration
      error_file: "{{ app_dir }}/logs/err.log",
      out_file: "{{ app_dir }}/logs/out.log",
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      merge_logs: true,

      // Restart strategy
      restart_delay: 4000,
      max_restarts: 10,
      min_uptime: "5s",

      // Zero-downtime reload
      wait_ready: true,
      listen_timeout: 10000,
      kill_timeout: 5000,
    },
  ],
};
